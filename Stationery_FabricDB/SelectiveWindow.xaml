<Window x:Class="Stationery_FabricDB.SelectiveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Stationery_FabricDB"
        mc:Ignorable="d"
        Title="SelectiveWindow" Height="150" Width="300">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ComboBox x:Name="comboBox" Width="200" Margin="0,0,0,10">
            </ComboBox>

            <StackPanel Orientation="Horizontal">
                <Button Content="OK" Width="75" Margin="0,0,10,0" Click="OKButton_Click" IsDefault="True"/>
                <Button Content="Cancel" Width="75" Click="CancelButton_Click" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
